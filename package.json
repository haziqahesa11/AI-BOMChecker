{
  "name": "ai-bomchecker",
  "version": "1.0.0",
  "description": "BOM vs CRD Specification Checker - monorepo (BackEnd + Frontend)",
  "private": true,
  "scripts": {
    "start": "npm start --prefix BackEnd",
    "dev": "npm run dev --prefix BackEnd",
    "build": "npm run build --prefix Frontend"
  }
}
