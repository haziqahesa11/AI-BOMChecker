{
  "name": "ai-bomchecker",
  "version": "1.0.0",
  "description": "BOM vs CRD Specification Checker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"cd client && npm run dev\"",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mssql": "^10.0.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
